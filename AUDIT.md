# جرد نظام العقود — مشروع غين (AUDIT)

> تاريخ الجرد: 2026-07-31 — المرحلة 1 من مشروع تطوير لوحة تحكم العقود.

## 1) التقنيات المستخدمة

| الجانب | الحالة |
|---|---|
| الأساس | موقع ثابت: HTML/CSS/JS خام — **بلا إطار عمل، بلا خطوة بناء، بلا backend** |
| الصفحات | `index.html` (الموقع التسويقي)، `slides.html` (عرض الشرائح)، `contract.html` (العقد الإلكتروني)، `interactive.html` (تحويلة قديمة) |
| المكتبات | مكتبة واحدة مضمّنة محليًا: `assets/lucide.min.js` (أيقونات lucide v1.25) |
| الخطوط | Cairo (عناوين) + Tajawal (نصوص) — ملفات woff2 محلية في `assets/fonts/` |
| اللغة والاتجاه | عربي بالكامل، `dir="rtl"` مثبّت في كل الصفحات |
| تخزين البيانات | **`localStorage` فقط** — لا قاعدة بيانات، لا API، لا أي اتصال شبكي |
| تصدير PDF | عبر قالب طباعة المتصفح (`window.print()` + `@media print`) — مُتحقق منه (13 صفحة A4) |
| تصدير DOCX | مكتبة `docx` مضمّنة محليًا (`assets/docx.iife.js`) + مولّد `assets/contract-docx.js` |
| النشر | GitHub Pages (عام) — صفحات العقد والإدارة `noindex` وغير مربوطة من الموقع |
| خادم التطوير | `python -m http.server` (منفذ 8899 في `.claude/launch.json`) |

## 2) وضع النسخ في Git (اكتشاف مهم)

وُجدت **ثلاث نسخ متفاوتة** من نظام العقود:

1. **نسخة `main`** (وكانت في مساحة العمل الحالية): عقد واحد، 19 بندًا، طباعة فقط — **الأقدم**.
2. **فرع `contract-v2-sign-and-exports`** (PR ‏#1 مفتوح): 21 بندًا، بيانات غين الرسمية مقفلة، توقيع غين المسبق (صورة)، تصدير PDF/DOCX.
3. **عمل غير محفوظ في Git داخل `D:\Claude\Gain`** — **الأحدث والأساس المعتمد للبناء** (قرار معتمد): يضيف `admin.html` وقارئ إعدادات الروابط `#c=` في `contract.html`.

> ⚠️ النسخة الأحدث كانت معرّضة للضياع (غير مرسلة إلى Git وغير مضمّنة في PR ‏#1). أول خطوة تنفيذية هي نسخها إلى مساحة العمل هذه وتأمينها.

## 3) الميزات الموجودة فعليًا (في النسخة المعتمدة)

### صفحة العقد `contract.html`
- عقد خدمات نظام غين — 21 بندًا + ملاحق (ملحق النطاق، ملحق SLA بجدول أزمنة استجابة).
- حقول تعبئة داخل النص (بيانات العميل، التواريخ) مع حفظ تلقائي في المتصفح وإشعار «حُفظت بياناتك».
- حاسبة اشتراك حية: 2,499 ريال/فرع/شهر + تطبيق اختياري 500 ريال + ضريبة 15%، مع دورات فوترة (1/3/6/12 شهرًا).
- توقيع غين مسبق (صورة معتمدة) + لوحة توقيع بالإصبع/الفأرة للعميل (canvas).
- رقم عقد مشتق تلقائيًا من اسم العميل والتاريخ.
- تصدير: زر «طباعة / حفظ PDF» (قالب A4 احترافي) + زر «حفظ DOCX».
- قارئ إعدادات من الرابط `#c=`: صيغة `p0` (مضغوطة) و`p1` (مشفّرة AES-GCM-256 بكلمة مرور، PBKDF2) مع شاشة فتح قفل — يطبّق تعديلات البنود، الأسعار، اسم العميل، ومعرّف العقد.

### لوحة الإدارة الأولية `admin.html`
- محرر أسعار (الاشتراك الأساسي، التطبيق، الضريبة).
- محرر بنود العقد (تحرير مباشر لكل بند مع شارة «معدّل» وزر استعادة الأصل، وحفظ كأساس).
- مولّد روابط عقود مخصصة (مع كلمة مرور اختيارية) + سجل الروابط المولّدة.

## 4) المشاكل المكتشفة

1. **خلل التقويم**: تاريخ الإبرام المعروض يظهر **هجريًا** بينما الإدخال ميلادي (`ar-SA` يفعّل تقويم أم القرى تلقائيًا) — تعارض شبه مؤكد غير مقصود.
2. **رقم العقد قابل للتصادم**: مشتق من hash بـ1000 احتمال فقط لكل يوم — ليس معرّفًا موثوقًا.
3. **لا تحقق من الإدخال**: يمكن طباعة عقد فارغ 0%؛ شريط الاكتمال شكلي، وخانة الموافقة غير مُلزمة قبل الطباعة.
4. **كود مكرر**: منطق الضغط/التشفير مكرر بين `admin.html` و`contract.html`.
5. **ملاحظات ثانوية**: حفظ مزدوج في حقل الفروع، إزاحة شريط الاكتمال ثابتة تنكسر عند التفاف الشريط العلوي على الشاشات الضيقة.

## 5) الفجوات (ما لا يوجد إطلاقًا)

- **لا لوحة عقود**: لا قائمة، لا جدول، لا بحث/فلترة، لا إحصائيات.
- **عقد واحد فقط**: تعبئة عقد لعميل جديد تمسح عقد العميل السابق؛ «تفريغ الحقول» هدّام بلا أرشيف.
- **لا مسودات** قابلة للاستئناف، و**لا حالات** للعقد (مسودة/قيد المراجعة/موقّع/منتهي).
- **لا تنبيهات انتهاء** ولا تواريخ إنشاء/تحديث.
- **لا معالج خطوات** — النموذج الحالي صفحة واحدة طويلة.
- **لا مسار رجوع للنسخة الموقّعة**: توقيع العميل يبقى في متصفحه فقط ولا يصل للشركة (لا backend).
- **لا نسخ احتياطي**: مسح بيانات المتصفح يُفقد كل شيء.
- **لا حالات فارغة** إرشادية ولا رسائل خطأ واضحة.

## 6) قيود يجب الانتباه لها

- `localStorage` مرتبط بمتصفح وجهاز واحد — بدون backend لا يمكن مزامنة تلقائية بين الأجهزة، ويُعوَّض بنسخ احتياطي يدوي (تصدير/استيراد ملف).
- حالة «موقّع» لا يمكن أن تتحدث تلقائيًا من متصفح العميل — تحتاج تعليمًا يدويًا أو استيراد ملف نسخة موقّعة.
- الموقع منشور علنًا على GitHub Pages؛ صفحات العقد والإدارة محمية فقط بكونها غير مفهرسة وغير مربوطة، وروابط العقود الحساسة تُحمى بكلمة مرور (تشفير `p1`).
