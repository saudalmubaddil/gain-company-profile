# خطة تطوير لوحة تحكم العقود — مشروع غين (PLAN)

> المرحلة 2 — مبنية على [AUDIT.md](AUDIT.md). القرارات المعتمدة: البناء على النسخة المتقدمة، تصدير PDF عبر طباعة المتصفح، قالب واحد (عقد الخدمات) مع بنية قابلة للتوسعة.

## الهدف

تحويل `admin.html` من صفحة أدوات بسيطة إلى **لوحة تحكم عقود متكاملة** واضحة لموظف غير تقني: إنشاء عقود متعددة عبر معالج خطوات، متابعتها في جدول بحالات وإحصائيات، وحفظ مسودات، مع الحفاظ الكامل على كل الميزات والبيانات الحالية.

## البنية العامة

- `admin.html` — لوحة تحكم من صفحة واحدة بأقسام (توجيه عبر `#/`):
  - `#/home` الرئيسية: إحصائيات + تنبيهات انتهاء + جدول العقود.
  - `#/new` معالج عقد جديد (و`#/new/<id>` لاستئناف مسودة).
  - `#/contract/<id>` تفاصيل عقد وإجراءاته.
  - `#/settings` الإعدادات: محرر الأسعار + محرر البنود (كما هما) + النسخ الاحتياطي.
- `contract.html` — تبقى صفحة العميل (هدف الرابط + مصدر المعاينة الحية).
- وحدات مشتركة جديدة: `assets/contract-link.js` (ضغط/تشفير الروابط — يزيل التكرار الحالي) و`assets/admin/store.js` (التخزين والترحيل).

## التخزين (localStorage — غير هدّام)

- `gain-dash-index`: فهرس خفيف لكل العقود `{id, status, counterparty, contractNo, createdAt, updatedAt, startDate, expiryDate, ...}`.
- `gain-dash-rec:<id>`: السجل الكامل (الحقول + إعدادات الرابط + سجل الحالات).
- `gain-dash-rec:<id>:sig`: توقيع العميل (منفصل لأنه ثقيل، يُحمّل عند الطلب).
- **ترحيل آمن**: عند أول تشغيل تُحوَّل روابط `gain-admin-history` القديمة إلى صفوف في الجدول بحالة «قيد المراجعة»، مع **إبقاء المفتاح القديم كما هو** كشبكة أمان. المفاتيح القائمة (`gain-contract-v2*`، `gain-admin-baseline`) تبقى تعمل بلا تغيير.
- كل عملية حفظ محمية من امتلاء التخزين برسالة عربية واضحة تشرح الحل.

## حالات العقد

| الحالة | اللون | كيف تحدث |
|---|---|---|
| مسودة | رمادي | أي عقد بدأ في المعالج ولم يُولَّد رابطه |
| قيد المراجعة | كهرماني | تلقائيًا عند توليد رابط العقد (أُرسل للعميل) |
| موقّع | أخضر | يدويًا («تعليم كموقّع») أو باستيراد ملف النسخة الموقّعة من العميل |
| منتهي | أحمر | **يُشتق تلقائيًا** عند تجاوز تاريخ الانتهاء (+ خيار «منتهي يدويًا» للعقود المفسوخة) |

«تنتهي قريبًا» = خلال 30 يومًا (توافق مهلة الإشعار في المادة 4 من العقد).

> ملاحظة صراحة: بلا خادم لا يمكن أن تتحدث حالة «موقّع» تلقائيًا من متصفح العميل؛ الحل المعتمد: زر «تصدير نسخة موقّعة» في صفحة العقد ينزّل ملفًا صغيرًا يستورده الموظف في اللوحة.

## بنود التنفيذ بالترتيب (المرحلة 3)

كل بند يُتحقق منه في المتصفح عبر الخادم المحلي قبل الانتقال للتالي.

1. **تأمين الأساس المتقدم** — نسخ `contract.html`, `admin.html`, `assets/contract-docx.js`, `assets/docx.iife.js`, `assets/gain-signature.png`, `assets/gain-logo-navy.png`, `docs/PRD-contract.md` من `D:\Claude\Gain` إلى مساحة العمل (استبدال النسخة القديمة — معتمد)، وإصلاح خلل التقويم (عرض ميلادي يطابق الإدخال).
   *تحقق:* العقد يفتح، الحاسبة تعمل، DOCX ينزل، رابط عادي ومشفّر يفتحان.
2. **استخراج الوحدات المشتركة** (`contract-link.js`, `store.js`) بلا أي تغيير في السلوك.
   *تحقق:* توليد رابط بكلمة مرور وفتحه — سلوك مطابق تمامًا.
3. **هيكل اللوحة والتنقل** — التوجيه بين الأقسام، شريط تنقل عربي واضح (الرئيسية / عقد جديد / الإعدادات)، نقل محرري الأسعار والبنود إلى قسم الإعدادات كما هما.
   *تحقق:* كل الميزات القديمة تعمل من الإعدادات؛ التنقل يعمل على شاشة جوال 375px.
4. **الرئيسية** — بطاقات إحصائيات (إجمالي العقود / تنتهي قريبًا / مسودات)، جدول عقود ببحث (اسم العميل/رقم العقد) وفلاتر (الحالة/التاريخ)، شارات حالات ملونة، شريط تنبيه للعقود القريبة من الانتهاء، حالة فارغة بشرح وزر «أنشئ أول عقد»، وترحيل سجل الروابط القديم.
   *تحقق:* الحالة الفارغة تظهر مع تخزين نظيف؛ الروابط القديمة تظهر كصفوف بعد الترحيل.
5. **تفاصيل العقد** — صفحة لكل عقد: الحالة والتواريخ والرابط، إجراءات (نسخ الرابط / فتح / تغيير الحالة / حذف مع تأكيد)، وكل توليد رابط يكتب سجلًا في الجدول.
   *تحقق:* توليد ← صف جديد ← تفاصيل ← الحالة «منتهي» تُشتق عند وضع تاريخ انتهاء بالأمس.
6. **معالج العقد الجديد (4 خطوات)** — (1) نوع العقد (بطاقة عقد الخدمات، بنية قابلة لإضافة قوالب لاحقًا) ← (2) بيانات العميل والاشتراك مع نصوص مساعدة تحت الحقول وتحقق فوري برسائل واضحة ← (3) **معاينة حية** للعقد كما سيراه العميل ← (4) الإنشاء: توليد الرابط (مع كلمة مرور اختيارية)، نسخ بضغطة، ورسالة نجاح مؤكدة. حفظ تلقائي كمسودة في كل خطوة + زر «حفظ كمسودة والخروج».
   *يتطلب تعديلين صغيرين في `contract.html`:* وضع معاينة (بلا حفظ تلقائي) وتعبئة مسبقة لكل حقول العميل من الرابط (لا الاسم فقط).
   *تحقق:* ملء نصفي ← خروج ← استئناف من الجدول؛ المعاينة تعكس الأسعار والبنود؛ الرابط الناتج يفتح معبّأً.
7. **دورة النسخة الموقّعة** — زر «تصدير نسخة موقّعة» في صفحة العقد (يفعَّل بعد الموافقة والتوقيع)، و«استيراد نسخة موقّعة» في تفاصيل العقد (مطابقة المعرّف، حفظ التوقيع، الحالة ← موقّع).
   *تحقق:* توقيع كعميل ← تصدير ← استيراد ← «موقّع» مع ظهور التوقيع؛ ملف غير مطابق يعطي رسالة خطأ واضحة.
8. **النسخ الاحتياطي والصقل النهائي** — تصدير/استيراد نسخة احتياطية JSON من الإعدادات، رسائل نجاح/خطأ موحدة لكل عملية، تجاوب كامل للجوال (الجدول يتحول لبطاقات على الشاشات الضيقة)، ثم تحديث `README.md` بالعربية: طريقة التشغيل وشرح كل قسم للمستخدم النهائي.
   *تحقق:* سيناريو كامل من عقد جديد حتى الاستيراد الموقّع + استعادة نسخة احتياطية + فحص الجوال.

## ما لن يتغير

- التقنيات الحالية (لا إطار عمل، لا backend، لا مكتبات جديدة).
- نظام التصميم (الألوان الكحلية/السماوية، الخطوط، الأزرار، الحقول) — توحيد كامل عبر اللوحة.
- صفحة العقد للعميل وقالب الطباعة وDOCX — تبقى كما هي عدا التعديلين المذكورين في البند 6 وزر التصدير في البند 7.
- أي بيانات محفوظة حاليًا في المتصفح.

## قواعد عامة أثناء التنفيذ

- كل نص للمستخدم بالعربية الفصحى المبسطة، بلا مصطلحات تقنية.
- الأولوية: وضوح التجربة ← اكتمال الميزات ← التحسينات الجمالية.
- لا رفع (commit/push) إلا بعد موافقة صريحة.
